// TODO: add type for maze
// TODO: fix type for algorithm (should be enum)
type Props = {
  maze: any
  algorithm: string
}

export function Grid({ maze, algorithm }: Props) {
  return <div>grid</div>
}
